import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
import 'models/organisation.dart';

const String giphyApiKey = ''; // Optional: insert Giphy api key here

const List<Organisation> organisations = [
  Organisation(
    id: 1,
    name: 'Die Kantanky\'s', // Insert your organisation name
    url: 'https://stam.diekantankys.nl', // Insert website url here
    apiKey: '', // Insert api key here
  ),
];

// Optional: set to FirebaseOptions(...) to enable push notifications
const FirebaseOptions? firebaseAndroidOptions = null;
const FirebaseOptions? firebaseIosOptions = null;
